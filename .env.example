# General Settings
APP_ENV=development
SERVER_PORT=8080
DEFAULT_TIMEOUT=15m

# Database Settings
# Format: user:password@tcp(host:port)/dbname?parseTime=true
MYSQL_DSN=root:@tcp(localhost:3306)/cashonrails?parseTime=true

# Storage Settings
# Dev: local | Prod: s3
STORAGE_TYPE=local
LOCAL_STORAGE_PATH=./exports

# S3 Settings (If STORAGE_TYPE=s3)
S3_BUCKET=my-export-bucket
AWS_REGION=us-east-1
S3_ENDPOINT=
S3_PATH_STYLE=false

# Email Settings
# Leave SMTP_HOST empty to disable real emails (logs to terminal)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@example.com

# Security Settings
# Use `go run scripts/gen_secret/main.go` to generate this for Prod
API_SECRET=devsecret
COMPRESSION=false
EMAIL_ATTACH_FILE=true

# Advanced
WORKER_COUNT=5
MAX_DB_CONCURRENCY=3
